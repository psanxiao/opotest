# Configuración de base de datos (PostgreSQL en Docker local)
DATABASE_URL=postgresql://postgres:postgres@localhost:5435/opotest

# Puerto de ejecución del servidor backend
PORT=8001

# Ruta al Antigravity CLI (opcional; por defecto ~/.local/bin/agy)
# AGY_BIN=~/.local/bin/agy
